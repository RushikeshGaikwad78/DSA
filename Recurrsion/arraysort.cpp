#include <bits/stdc++.h>
using namespace std;


void sort(vector<int> arr){
    if(arr.size()==1){
        return;
    }
    // arr.push_back(5);
    // cout<<"in function"<<endl;

    return;

}

int main(){
    vector<int> a = {4,7,3,2,5};
    vector<int>result;
    cout<<"before"<<endl;
    sort(a);
    cout<<"after"<<endl;
}